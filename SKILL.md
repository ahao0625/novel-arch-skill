---
name: novel-arch
description: |
  AI 网文创作项目操作手册。
  收到用户指令后，按对应操作流程执行，每步做完自检再继续。
  覆盖：新建项目、写章节、更新设定、审计、关卷五个核心操作。
agent_created: true
---

# Novel Arch — AI 操作手册

> 收到用户指令 → 查找对应操作 → 按步骤执行 → 每步自检 → 完成后报告。
> 所有路径均为项目根目录下的相对路径，参考 `01_世界设定/` 目录（题材适配时名称可能不同）。

---

## 操作速查

| 用户指令 | 对应操作 | 优先级 |
|:---|:---|---:|
| "新建项目"、"开新书"、"初始化" | 操作一：新建项目 | 最高 |
| "写第X章"、"继续写"、"开始写" | 操作二：写入章节 | 高 |
| "改设定"、"加个角色"、"修改世界观" | 操作三：更新设定 | 中 |
| "审计"、"检查"、"审稿" | 操作四：运行审计 | 中 |
| "关卷"、"卷结束"、"完成本卷" | 操作五：关卷存档 | 低 |

---

## 操作一：新建项目

### 触发条件
用户说：新建项目 / 开新书 / 启动新项目 / 按照架构创建 / 给我建一个小说项目

### 前置条件
1. 确认项目名、类型、卷数、目标字数（有则用，无则问）
2. 确认用户是否提供了大纲/设定文本（有则使用，无则建空骨架）

### 执行步骤

#### Step 1：创建目录
运行 `scripts/setup_project.py`，传入项目路径、类型、卷数。
→ 验证：项目根目录是否已生成、01~10 目录是否都在

#### Step 2：填充项目设置
→ `项目配置.md`：填入书名、类型、字数、卷数
→ `创建日志.md`（`08_创作日志/`）：创建空表格，只保留表头

#### Step 3：填充内容前判断

**情况 A：用户有完整大纲/设定文档**
```
执行流程：
  1. 逐段阅读大纲，按以下 6 类提取信息：
     - 世界观要素（力量体系/地理/势力/核心矛盾）
     - 角色（姓名/身份/性格/能力/动机/关键关系）
     - 剧情结构（卷划分/每卷一句话摘要/核心事件链）
     - 时间阶段（故事跨度/关键时间节点）
     - 悬念（核心悬念的埋设和回收计划）
     - 红线（已知的不可触碰规则/设定铁律）
  2. 每类信息填入对应文件（见下方文件映射表）
  3. 提取不到的信息留空，标注【待补充】，不自作主张编造
  4. 基于已提取信息完成一项初步的内容评估
```

**情况 B：用户只有简要创意（几句话）**
```
执行流程：
  1. 基于创意合理扩展，构建骨架
  2. 填入文件时，AI 补充的内容标注【AI建议，请确认】
  3. 保留大量空白让用户后续填充
```

**情况 C：用户只有书名**
```
执行流程：
  1. 创建空骨架文件（只建目录和空文件）
  2. 不自主填充任何内容
```

#### Step 4：按文件填充内容

对每个文件，严格按以下字段规范填充：

**01_世界设定/总纲.md**
| 字段 | 必填 | 填充规则 |
|:---|:---:|:---|
| 书名 | ✅ | 从用户确认的项目名 |
| 类型 | ✅ | 从用户确认的类型 |
| 目标字数 | ✅ | 从用户确认的字数 |
| 单章字数 | ✅ | 从用户确认的区间 |
| 核心主题 | ✅ | 从大纲提取；无则填【待补充】 |
| 核心矛盾 | ✅ | 从大纲提取；无则基于类型和主题推断并标注【AI推断】 |
| 力量/规则体系 | ✅ | 从大纲提取；无则留空 |

**01_世界设定/地理与势力.md**
| 字段 | 必填 | 填充规则 |
|:---|:---:|:---|
| 势力/区域列表 | ✅ | 从大纲提取 |
| 势力关系 | 推荐 | 从大纲提取；无则留空 |
| 各势力/区域说明 | 推荐 | 从大纲提取；无则留空 |

**01_世界设定/规则体系.md**
| 字段 | 必填 | 填充规则 |
|:---|:---:|:---|
| 体系名称 | ✅ | 从大纲提取；无则填【待设定】 |
| 等级/层级 | ✅ | 从大纲提取；无则填【待设定】 |
| 各等级说明 | 推荐 | 从大纲提取；无则留空 |

**01_世界设定/设定速查锚点.md**
| 字段 | 必填 | 填充规则 |
|:---|:---:|:---|
| 主角当前状态 | ✅ | 从大纲提取主角初始状态 |
| 当前卷/核心矛盾 | ✅ | 根据第一卷规划填充 |
| 势力关系快照 | ✅ | 从大纲提取；无则留空 |
| 活跃悬念 | 推荐 | 从大纲提取核心悬念 |

**01_世界设定/红线清单.md**
| 字段 | 必填 | 填充规则 |
|:---|:---:|:---|
| 角色红线 | 推荐 | 从大纲提取已明确不可触碰的规则 |
| 设定红线 | 推荐 | 从大纲提取已明确不可触碰的规则 |
| 情节红线 | 推荐 | 从大纲提取已明确不可触碰的规则 |

**01_世界设定/设定变更日志.md**
→ 初始化 v1.0 行，日期为当前日期，变更内容填"初始设定建立"

**02_人物档案/人物档案全集.md**
| 字段 | 必填 | 填充规则 |
|:---|:---:|:---|
| 角色姓名 | ✅ | 从大纲提取 |
| 身份 | ✅ | 从大纲提取 |
| 性格 | 推荐 | 从大纲提取；无则留【待补充】 |
| 能力 | ✅ | 从大纲提取；无则【待设定】 |
| 动机 | ✅ | 从大纲提取；无则可从剧情推断并标注【AI推断】 |
| 关键关系 | 推荐 | 从大纲提取；无则留空 |

**03_故事大纲/全书故事大纲.md**
| 字段 | 必填 | 填充规则 |
|:---|:---:|:---|
| 多卷结构 | ✅ | 从大纲提取卷划分 |
| 每卷一句话摘要 | ✅ | 从大纲提取；无则【待补充】 |
| 核心事件链 | ✅ | 从大纲提取 |

**03_故事大纲/第一卷·章节纲要.md**
| 字段 | 必填 | 填充规则 |
|:---|:---:|:---|
| 前 10 章规划 | ✅ | 有大纲章节粒度则填入；无则基于卷摘要规划前 3 章并标注【AI建议】 |
| 第 11 章起 | 可选 | 留空，标注【待填充】 |

**05_伏笔与悬念/伏笔与揭秘体系.md**
| 字段 | 必填 | 填充规则 |
|:---|:---:|:---|
| 伏笔编号（A/B/C） | ✅ | 从大纲提取核心悬念 |
| 类型/描述 | ✅ | 从大纲提取 |
| 预计回收卷/章 | 推荐 | 从大纲提取；无则留空 |

**06_正文内容/第一卷/卷概述.md**
| 字段 | 必填 | 填充规则 |
|:---|:---:|:---|
| 卷号 | ✅ | 根据卷划分 |
| 本卷核心事件 | ✅ | 从大纲提取 |
| 主角起止状态 | ✅ | 从大纲提取；无则基于剧情推断 |

**06_正文内容/第一卷/第1章.md**
→ 按"操作二：写入章节"的规范执行

#### Step 5：自检

```
□ 所有【必填】字段是否都已非空？
□ 提取不到的内容是否标注了【待补充】，没有自行编造？
□ AI 推断/建议的内容是否标注了【AI推断】/【AI建议】？
□ 角色档案中，大纲提到的每个主要角色都在？
□ 伏笔编号与纲要/正文之间的交叉引用是否一致？
□ 正文文件名格式是否为 第X章.md（不加书名前缀）？
□ 目录结构是否完整（01~10 都在）？
```

#### Step 6：输出报告

```
✅ 项目搭建完成

已填充：
  - 01_世界设定/——{填充情况}
  - 02_人物档案/——{N}个角色
  - 03_故事大纲/——{填充情况}

待补充：
  - [ ] {字段} — {说明}
  - [ ] {字段} — {说明}

自检结果：{通过/有警告，见上方清单}

下一步建议：
  1. {建议}
  2. {建议}
```

---

## 操作二：写入章节

### 触发条件
用户说：写第X章 / 继续写 / 开始写第X章 / 接着写 / 帮我写出第X章

### 前置条件
1. 确认当前卷号、章节号（从已有文件确定，或问用户）
2. 读取以下文件（必读 3 个）：
   - `01_世界设定/设定速查锚点.md` — 获取当前状态
   - `03_故事大纲/第X卷·章节纲要.md` — 获取本章规划
   - `08_创作日志/卷内章节摘要缓存.md` — 获取前情
3. 如果章节纲要中有本章规划，优先按规划写；如果没有，从上一章结尾自然续写

### 执行步骤

#### Step 1：准备章节元数据

```yaml
---
章号: {数字}
卷: {中文数字}
时间: {故事内时间}
地点: {场景名称}
主角状态: {等级/状态}
活跃角色: {本章出场角色}
本章核心: {一句话，不超过 20 字}
前情提要: {上一章末尾，1-2 句}
---
```

自检：
```
□ 章号 = 上一章章号 + 1（如果是新建章节）
□ 时间与上一章是否连续（没有无故跳跃）
□ 活跃角色的名字与人物档案中的一致
```

#### Step 2：执行四阶段写作流程

```
① 深度分析 → 阅读章节纲要，确定本章核心冲突和情绪曲线
② 结构规划 → 字数分配（起10% 承40% 转35% 合15%）
③ 优质撰写 → 逐段输出，遵守去AI痕迹规则
④ 自我质检 → 见下方质检清单
```

#### Step 3：写作规范

**去 AI 痕迹管控**
```
限频词（每 3000 字 ≤ 1 次）：
  不由得、脑海中、心头一震、猛然间、缓缓、淡淡、竟然、仿佛、忽然、似乎

禁止行为：
  - 情绪形容词堆砌（悲伤/痛苦/绝望不同框）
  - 分析式描写（动机用行为展示，不用"因为...所以..."）
  - 连续情绪转折（单段落 ≤ 2 次）
  - 把修炼/学习过程写进正文（用结果暗示）
  - 同一意象跨章重复
```

**字数管控**
```
目标：单章 2000-3000 字
不足时扩充方向：加感官细节、对话潜台词、过程细节
超标时精简方向：删冗余修饰词、合并同义重复、移除分析式旁白
```

#### Step 4：自我质检

```
□ 字数是否在目标区间内？（执行 wc -m 确认）
□ 限频词是否超标？（grep 确认 ≤1次/3000字）
□ 设定与速查锚点中的当前状态是否一致？
□ 角色行为是否符合其档案中的性格？
□ 章末是否留有钩子？（悬念/暗示/回收/升级 四选一）
□ 正文是否标记了伏笔引用（如：（伏笔 A-2，埋于本章））？
```

#### Step 5：写作后维护

顺序执行以下 5 步：

```
1. 保存正文 → 06_正文内容/第X卷/第X章.md
2. 写日志 → 08_创作日志/第X卷创作日志.md（追加一行：章号/日期/字数/完成度/小结/伏笔状态）
3. 写摘要 → 08_创作日志/卷内章节摘要缓存.md（追加：第X章—1-3句话）
4. 更新伏笔 → 05_伏笔与悬念/伏笔与揭秘体系.md（埋设的加条目，回收的标状态）
5. 更新索引 → 07_章节索引/第X卷索引.md（追加：第X章|标题|字数|✅完成）
```

自检：
```
□ 正文文件保存路径是否正确？
□ 日志是否追加了新行？
□ 伏笔表是否更新了本章埋设或回收的条目？
□ 章节索引是否与本卷其他章格式一致？
```

---

## 操作三：更新设定

### 触发条件
用户说：修改/添加/更新设定 / 加个角色 / 改世界观 / 加一条规则

### 前置条件
1. 读取当前 `01_世界设定/` 下的相关文件，确认当前状态
2. 判断：这是"新增"还是"修改"？

### 执行步骤

#### Step 1：定位修改目标

| 用户说的内容 | 修改哪个文件 |
|:---|:---|
| 修改力量体系/规则 | `01_世界设定/规则体系.md` |
| 修改地理/势力 | `01_世界设定/地理与势力.md` |
| 新增/修改角色 | `02_人物档案/人物档案全集.md` |
| 新增/修改悬念 | `05_伏笔与悬念/伏笔与揭秘体系.md` |
| 新增铁则/红线 | `01_世界设定/红线清单.md` |

#### Step 2：执行修改

执行修改后，必须依次做以下 3 件事：

```
1. 更新对应文件（直接编辑）
2. 更新设定速查锚点（如果修改影响了"当前状态"或"势力关系"）
3. 记录变更日志（01_世界设定/设定变更日志.md）
   | v1.x | 日期 | 变更内容 | 变更原因 | 影响范围 |
```

#### Step 3：自检

```
□ 修改是否已同步到速查锚点的"近况变化"中？
□ 变更日志是否已记录？（版本号 = 当前最高 v + 1）
□ 受影响的文件是否跨目录存在多个副本需要同步？
  （例如：修改角色性格 → 人物档案和速查锚点都需要更新）
```

---

## 操作四：运行审计

### 触发条件
用户说：审计 / 检查 / 审稿 / 质量检查 / 每10章了查一下

### 前置条件
1. 确认审计范围（默认最近 10 章，可指定范围）
2. 读取以下文件：
   - `01_世界设定/红线清单.md`
   - `04_时间线/完整时间线.md`
   - `05_伏笔与悬念/伏笔与揭秘体系.md`
   - 审计范围内的正文章节

### 执行步骤

#### Step 1：逐项检查

| 层级 | 检查项 | 检测方法 |
|:---:|:---|---:|
| 🔴 致命 | 设定冲突 | 对照 01_世界设定/ 中的规则，检查正文是否违反 |
| 🔴 致命 | 角色 OOC | 对照 02_人物档案/，检查角色言行是否符合档案 |
| 🔴 致命 | 能力超纲 | 对照 01_世界设定/规则体系.md，角色是否使用了未解锁的能力 |
| 🟡 警告 | 悬念健康 | 伏笔表中是否有超过 10 章未提及的伏笔 |
| 🟡 警告 | 节奏检查 | 连续章节的情绪曲线是否单一（建议每 5 章切换一次） |
| 🟢 提示 | 格式规范 | 章节元数据是否完整、文件名是否符合规范 |

#### Step 2：红线违规检测

逐条检查红线清单，确认审计范围内是否有违规。如果有 → 标记 🔴 阻断级，必须修复。

#### Step 3：输出审计报告

保存到 `09_审计报告/第X卷/第X卷审计报告（第X-X章）.md`

报告格式：
```
## 第X卷审计报告（第X-X章）

### 🔴 Critical
| 序号 | 问题 | 位置 | 修复方案 |

### 🟡 Warning
| 序号 | 问题 | 位置 | 建议 |

### 🟢 Info
| 序号 | 问题 | 位置 |

### 伏笔状态
| A/B/C | 埋设中 | 已回收 | 超期 |

### 设定核对
| 检查项 | 状态 | 是否一致 |

### 结论
- 阻断级：{有/无}
- 待修复：{清单}
- 建议：{建议}
```

---

## 操作五：关卷存档

### 触发条件
用户说：本卷写完了 / 关卷 / 完成第X卷 / 卷结束

### 前置条件
1. 确认卷号
2. 确认本卷的最后一章已保存

### 执行步骤

```
1. 生成设定快照（01_世界设定/设定快照_第X卷.md）
   → 汇总本卷结束时主角状态、世界观状态、已完成事件、活跃伏笔、待办事项
2. 刷新设定速查锚点（01_世界设定/设定速查锚点.md）
   → 更新当前卷为下一卷
3. 完善卷概述（06_正文内容/第X卷/卷概述.md）
   → 补充总字数、创作周期、本卷总结
4. 更新总索引（07_章节索引/总索引.md）
   → 追加本卷的总字数、章节数
```

### 自检

```
□ 设定快照是否已生成？（含主角状态/世界观/伏笔/待办）
□ 速查锚点的"当前卷"是否已更新为下一卷？
□ 卷概述中是否补充了字数和总结？
□ 变更日志中是否有未记录但实际已发生的变更？
```

---

## 通用规则（所有操作遵循）

### 内容标注规则

| 标注 | 含义 | 使用场景 |
|:---|:---|---:|
| 【待补充】 | 用户后续自行补充 | 必填字段无数据时 |
| 【待设定】 | 需要在开写前决定 | 核心设定（如力量体系）无数据时 |
| 【AI推断】 | 基于现有信息合理推断 | 必填字段无数据但可从上下文推导 |
| 【AI建议，请确认】 | AI 补充的内容需要用户审核 | 用户只给创意骨架时 |

### 文件操作规则

```
- 正文文件名：第X章.md（不加书名前缀）
- 设定文件名：不加书名前缀
- 不删除已有文件，只追加/编辑
- 编辑前先读取当前内容，确认修改范围
- 每次修改后更新变更日志
```

### 审计触发节点

```
- 每完成 10 章 → 自动触发审计（操作四）
- 卷结束时 → 自动触发关卷（操作五）
- 用户主动要求 → 随时触发审计
```
